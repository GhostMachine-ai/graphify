interface:
  display_name: "Graphify"
  short_description: "Repo-specific patterns and workflows for graphify"
  default_prompt: "Use the graphify repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true